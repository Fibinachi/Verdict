<Window x:Class="Verdict.Views.CaseSelectionDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Verdict.Views"
        mc:Ignorable="d"
        Title="New Case" Height="380" Width="550"
        WindowStartupLocation="CenterOwner" ResizeMode="CanResize"
        Background="#2C3E50" Foreground="White">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Select a demo case to load:" FontWeight="Bold" FontSize="14" Margin="0,0,0,10"/>

        <ListBox x:Name="CaseList" Grid.Row="1" Margin="0,0,0,15"
                 Background="#34495E" Foreground="White" BorderBrush="#1A252F"
                 FontSize="13" SelectionMode="Single">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Margin="5">
                        <TextBlock Text="{Binding DisplayName}" FontWeight="Bold" FontSize="14"/>
                        <TextBlock Text="{Binding Description}" FontSize="11" Foreground="#BDC3C7" Margin="0,2,0,0"/>
                        <TextBlock Text="{Binding CaseType}" FontSize="10" Foreground="#E67E22" Margin="0,2,0,0"/>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Open Selected" Click="OpenSelected_Click" Width="110" Padding="8,5"
                    Background="#2ECC71" Foreground="White" FontWeight="Bold" IsDefault="True"/>
            <Button Content="Blank Case" Click="BlankCase_Click" Width="100" Padding="8,5" Margin="10,0,0,0"
                    Background="#3498DB" Foreground="White"/>
            <Button Content="Cancel" Click="Cancel_Click" Width="80" Padding="8,5" Margin="10,0,0,0"/>
        </StackPanel>
    </Grid>
</Window>
